version: '3.1'
services:
  db:
    image: mariadb
    restart: always
    environment:
      MYSQL_DATABASE: todo
      MYSQL_USER: todo
      MYSQL_PASS: mysupersecretpassword
    ports:
      - 3306:3306
